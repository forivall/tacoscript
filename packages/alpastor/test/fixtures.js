require('source-map-support').install()

const alpastor = require('../lib');

suite('alpastor', function () {
  // test('smoke', function () {
  //   alpastor.generate({type: 'Program', sourceType: 'script', body: [], directives: [], tacoscriptSourceElements: []})
  // })
})
