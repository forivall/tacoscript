export default {
  sourceType: {
    description: "Declares whether the code should be parsed as a script or a module",
    default: "module"
  },

  generate: {
    description: "arguments to pass into the generator (babel-generator by default)"
  }
};
